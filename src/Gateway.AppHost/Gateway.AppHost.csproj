<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UserSecretsId>070af733-a5e4-4a63-b2bc-20de3a35d97d</UserSecretsId>
    <DockerDefaultTargetOS>Linux</DockerDefaultTargetOS>
    <DockerfileContext>..\..</DockerfileContext>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.VisualStudio.Azure.Containers.Tools.Targets" Version="1.23.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- Adds the `schema export` verb to this host, which is how CI produces schema.graphql for
         the frontend's codegen. Only meaningful together with RunWithGraphQLCommandsAsync in
         Program.cs; without that call the argument is parsed by nobody and the host just starts. -->
    <PackageReference Include="HotChocolate.AspNetCore.CommandLine" Version="16.6.1" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Serilog.AspNetCore" Version="10.0.0" />
    <PackageReference Include="Serilog.Enrichers.Environment" Version="3.0.1" />
  </ItemGroup>

  <ItemGroup>
    <!-- Stable only. The EF Core instrumentation and the Prometheus AspNetCore exporter have
         never shipped a stable release, so traces come from the EF ActivitySource directly and
         metrics leave over OTLP for a collector to expose to Prometheus. -->
    <PackageReference Include="OpenTelemetry.Extensions.Hosting" Version="1.18.0" />
    <PackageReference Include="OpenTelemetry.Instrumentation.AspNetCore" Version="1.18.0" />
    <PackageReference Include="OpenTelemetry.Instrumentation.Http" Version="1.18.0" />
    <PackageReference Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" Version="1.18.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Gateway.Infrastructure\Gateway.Infrastructure.csproj" />
    <ProjectReference Include="..\Gateway.Presentation\Gateway.Presentation.csproj" />
  </ItemGroup>

</Project>
