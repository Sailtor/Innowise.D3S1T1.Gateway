**/.classpath
**/.dockerignore
**/.env
**/.git
**/.gitignore
**/.project
**/.settings
**/.toolstarget
**/.vs
**/.vscode
**/*.*proj.user
**/*.dbmdl
**/*.jfm
**/azds.yaml
**/bin
**/charts
**/docker-compose*
**/Dockerfile*
**/node_modules
**/npm-debug.log
**/obj
**/secrets.dev.yaml
**/values.dev.yaml
LICENSE
README.md
!**/.gitignore
!.git/HEAD
!.git/config
!.git/packed-refs
!.git/refs/heads/**

# --- Gateway-only additions (the rest of this file matches the sibling services verbatim) ---

# The Dockerfile builds src/Gateway.AppHost, never the solution, so neither of these reaches the
# image. Excluding them keeps the `COPY . .` layer from being invalidated by a test or workflow
# edit, which is most of the churn in this repo.
tests
.github

# Documentation, for the same cache reason. The verbatim block above already excludes README.md by
# name; this generalises it, so a new .md file cannot silently start invalidating the build layer.
**/*.md
